{{ team }} команда, {{ district }} округ
Общее число: <b>{{ participants | length }}</b>

<b>Кураторы:</b>
{% for curator in curators %}<pre>{{ "%4d" | format(curator.id) }}</pre> {{ curator.full_name }}
{% endfor %}
<b>Участники:</b>
{% for prt in participants %}<pre>{{ "%4d" | format(prt.id) }}</pre> {{ prt.display_name }}
{% endfor %}
